const {Pokemon} = require('../pokemon.js');
const {expect} = require('chai');

describe('Pokemon',() => {
    it('Creates a new Pokemon with 6 properties',() => {
    expect('s').to.equal('a');
    });
});